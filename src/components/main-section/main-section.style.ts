import { css } from '@emotion/react'
import styled from '@emotion/styled'

export const SectionStyled = styled.section(() => css`
    margin-top: 80px
`)

export const TopSectionStyled = styled.div(() => css`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 40px
`)
